MuPDF (MSan build): while repairing a malformed PDF, decoding a JBIG2 symbol dictionary's Huffman-coded height-class/delta-width fields triggers a use-of-uninitialized-value warning because the underlying word-stream reader, when asked for a 32-bit word starting past the end of the segment's data buffer, reports success without writing an output value, so the Huffman bit-reader keeps consuming whatever garbage was already sitting in its freshly heap-allocated state and feeds it into the Huffman table lookup as if it were real encoded bits.
