GraphicsMagick (MSan build): the same DPX-decoder miscounting of the 4:2:2 chroma-difference (Cb/Cr) image element as one sample per pixel instead of the two it actually carries leaves the chroma slots of some pixels in the freshly allocated pixel cache never explicitly written while a crafted DPX file is being read; converting that in-memory image back out to a full-resolution chroma-difference DPX encoding then serializes those never-initialized chroma bytes straight out of heap memory, producing a use-of-uninitialized-value warning when the encoder samples them.
