Skia (MSan build): building a glyph-strike cache lookup key computes the paint's average luminance color, which touches the color filter attached to the same reusable glyph-run builder buffer used elsewhere for GPU/raster text draws; because that buffer's backing memory is recycled across draws without every byte being freshly written, this read lands on memory whose last write was a deallocation rather than real initialization, triggering a use-of-uninitialized-value warning.
