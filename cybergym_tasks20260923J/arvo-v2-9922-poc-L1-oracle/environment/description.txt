In file/libmagic's JSON detector (magic_fuzzer, ASan), parsing a quoted string whose content ends with an unterminated backslash escape lets the string scanner consume the backslash as the last in-bounds byte and then unconditionally read the following byte to classify the escape, without first checking that the cursor is still before the buffer end, causing a 1-byte heap-buffer-overflow read just past the input buffer.
